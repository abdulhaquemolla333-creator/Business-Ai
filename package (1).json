{
  "name": "business-ai-backend",
  "version": "1.0.0",
  "description": "Backend server for the Business AI app — forwards chat requests to the Anthropic API.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  }
}
